Installed packages:

- Universal RP (need to install)
- Input System (need to install)
- TextMeshPro (need to install)
- DOTween (HOTween v2) (need to install)

First you need to install Cars_URP. 
Also, scene 1 and scene 2 must be added to the build for the package to work properly. 
Switch on the rcc mobile controller to use draggin on scene 1.
